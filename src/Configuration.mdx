import { Meta } from "@storybook/addon-docs/blocks";

<Meta title='Getting Started/Configuration' />

# Storybook Configuration

To get started with Storybook, you'll need to configure it to work with your project. Below are the minimal steps required for configuration.

## 1. Install Storybook

First, install Storybook with the following command:

```bash
npx storybook init
```
